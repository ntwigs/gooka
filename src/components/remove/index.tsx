import remove from '../../assets/remove.svg'
import styled from 'styled-components'

const RemoveImage = styled.img``

export const Remove = () => (
  <RemoveImage src={remove} alt="Remove image from list" />
)
